# param(
#     [string]$Username,
#     [string]$Password
# )

Write-Output "=== PoC-Win-App Setup ==="
# Write-Output "Running as: $env:USERNAME"
# Write-Output "DB Username: $Username"

# --- Placeholder: Create DB tables ---
# Replace this with actual SQL commands when ready
# Example:
# $connStr = "Server=localhost;Database=PoCDB;User Id=$Username;Password=$Password;"
# $conn = New-Object System.Data.SqlClient.SqlConnection($connStr)
# $conn.Open()
# $cmd = $conn.CreateCommand()
# $cmd.CommandText = "CREATE TABLE IF NOT EXISTS ..."
# $cmd.ExecuteNonQuery()
# $conn.Close()

Write-Output "Database tables created (placeholder)"
Write-Output "Setup complete"